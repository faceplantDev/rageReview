1) npm i - внутри корневой папки.
2) npm i - внутри src_client_packages
3) webpack - внутри src_client_packages
4) Закинуть файлы:
    bin
    bugTrap-x64.dll
    ragemp-server.exe
В корневой каталог.